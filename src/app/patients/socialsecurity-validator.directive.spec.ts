import { SocialsecurityValidatorDirective } from './socialsecurity-validator.directive';

describe('SocialsecurityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialsecurityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
